{
  "name": "creator-scraper",
  "version": "1.5.0",
  "description": "Apify bio scraper + Claude matching + screenshot vision + Whisper transcripts + cost guardrail + CRM + outreach email pipeline",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node -c server.js && node -c db.js && node -c lib/extractors.js && node -c lib/cost.js && node -c lib/email_render.js && node -c lib/smtp.js && node -c workers/scraper.js && node -c workers/evaluator.js && node -c workers/linktree.js && node -c workers/vision.js && node -c workers/videos.js && node -c workers/email_generator.js && node -c workers/email_sender.js && node -c workers/imap_listener.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.30.1",
    "apify-client": "^2.10.0",
    "express": "^4.21.1",
    "imapflow": "^1.0.171",
    "mailparser": "^3.7.1",
    "multer": "^1.4.5-lts.1",
    "nodemailer": "^6.9.16",
    "pg": "^8.13.1",
    "playwright": "^1.49.1"
  }
}
